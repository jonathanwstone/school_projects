public interface GasEngine {

    // Interface tool to refuelTank
    double refuelTank(double fuel);

    // Tool to get tank capacity
    double getTankCapacity();

    // Tool to get current amount of fuel
    double getCurrentVolume();
}
